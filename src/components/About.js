import { Link } from 'react-router-dom'

const About = () => {
    return (
        <div>
            <h4>server-side used: JSON Server</h4>     
             <Link to='/'>Go Back</Link>      
        </div>
    )
}

export default About
